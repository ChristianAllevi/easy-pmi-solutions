import { StaticImageData } from "next/image";

export type servicecard = { image: StaticImageData, title: string, description:string, color:string }
export type planscard = { icon: StaticImageData, title: string, catchword:string, description:string }
